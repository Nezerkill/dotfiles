# INPUT CONFIGURATION

input {
    kb_layout = us,ru
    kb_options = grp:alt_shift_toggle
    kb_variant =
    kb_model =
    kb_rules =
    follow_mouse = 1
    accel_profile = flat
    repeat_rate = 50
    repeat_delay = 300

    touchpad {
        natural_scroll = false
    }

    sensitivity = 0
}
